server:
  servlet:
    context-path: /api

spring:
  application:
    name: api-totp-demo

beom:
  payload:
    dto:
      qrcode:
        request:
          path: payload/QRCodeRequest.json
        response:
          path: payload/QRCodeResponse.json
      totp:
        request:
          path: payload/TotpRequest.json
        response:
          path: payload/TotpResponse.json